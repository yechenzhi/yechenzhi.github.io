# Classical CUDA SGEMM tutorial posts

Tutorial articles for this series belong in this directory, not under `_pages`.

Use dated Jekyll post filenames and include `series: classical-cuda-sgemm` plus a numeric `episode` value in each post's front matter so the series page can list them in order.
